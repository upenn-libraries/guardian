version: '3.1'

secrets:
  db_secrets:
    file: 'db.yml'
  glacier_secrets:
    file: 'glacier.yml'
  guardian_secrets:
    file: 'guardian.yml'
  bulwark_gitannex_secrets:
    file: 'bulwark_gitannex.yml'

services:
  db:
    image: 'guardian_db:5.7'
    secrets:
      - db_secrets
    volumes:
      - 'db:/var/lib/mysql'
  guardian:
    image: 'guardian:latest'
    depends_on:
      - 'db'
    secrets:
      - db_secrets
      - glacier_secrets
      - guardian_secrets
      - bulwark_gitannex_secrets
    volumes:
      - 'guardian:/usr/src/app'
      - 'guardian_todos:/todos'

volumes:
  db:
  guardian:
  guardian_todos:
